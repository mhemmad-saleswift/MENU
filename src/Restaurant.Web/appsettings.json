{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Admin": {
    "Username": "admin",
    "//Password": "Set via the Admin__Password environment variable. Required in Production; defaults to 'admin123' only in Development."
  }
}
